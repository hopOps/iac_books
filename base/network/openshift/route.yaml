apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: app-books
  name: app-books
spec:
  host: app-books-lab-collen.apps.local
  port:
    targetPort: 5000
  to:
    kind: Service
    name: app-books
    weight: 100
  tls:
    termination: edge
